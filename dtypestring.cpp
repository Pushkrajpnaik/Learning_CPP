#include<iostream>
#include<string>
using namespace std;
int main()
{
    //**string and getline
    // string str;
    // getline(cin,str);
    // cout<<"the entered striing is: ";
    // cout<<str;

    //**char- will always give output of one character only
    char ch;
    cout<<"enter a single character ";
    cin>>ch;
    cout<<ch;
   return 0;
}